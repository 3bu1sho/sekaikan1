<div class='container px-5 px-sm-0'>
  <div class='row'>
      <div class="col-md-11 offset-md-1 px-5 px-sm-0 mx-auto">
      <div>
      <% if @sekai.errors.any? %>
        <%= @sekai.errors.count %>件のエラーが発生しました
        <% @sekai.errors.full_messages.each do |message| %>
          <%= message %>
   　　   <% end %>
      <% end %>
      <h1>新規投稿</h1>
      </div>    <!-- form_with部分 -->
      <%= form_with model: @sekai, url:'/sekais', local:true do |f| %>
        <h4>画像</h4>
        <%= f.attachment_field :image %>
        <h4>世界の名</h4>
        <%= f.text_field :name %>
        <h4>概要</h4>
        <%= f.text_area :introduction, size: "100x10" %>
        
        <br>
        <%= "半角スペースを入力することで複数の要素を付けることができます。" %>
        </br>
        <br>
        <%= "例：SF スチームパンク ナーロッパ　地動説" %>
        <%= f.text_field :tag_name %>
        </br>
        <%= f.submit '創造' %>
      <% end %>
    </div>
  </div>
</div>