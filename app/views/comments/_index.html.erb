<% sekai.comments.each do |comment| %>
  <% comment_user = comment.user %>
  <tr>
  	<td>
  		<%#= link_to user_path(comment_user), class: "user_#{comment_user.id}" do %>
    		<%= comment_user.name %>
  		<%# end %>
  	</td>
  	<td><%= comment.comment %></td>
  	<% if current_user == comment_user %>
    	<td>
    	  <%#= link_to 'Destroy', comment_path(comment.sekai_id, comment.id), class: 'btn-sm btn-danger', method: :delete, remote: true %>
<%= link_to "削除", sekai_comment_path(comment.sekai_id, comment.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>    	</td>
  	<% end %>
  </tr>
<% end %>